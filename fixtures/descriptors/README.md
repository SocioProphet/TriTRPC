# Descriptor fixtures placeholder

This directory will hold deterministic fixture examples for descriptor carriage over TriTRPC.

Planned coverage:
- `GeneralDescriptor` registration request/response
- descriptor retrieval response
- descriptor update request/response
- schema/context identifier examples
