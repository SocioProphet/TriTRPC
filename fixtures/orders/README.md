# Order fixtures placeholder

This directory will hold deterministic fixture examples for order carriage over TriTRPC.

Planned coverage:
- `OrderDescriptor` create request/response
- order validation response
- order status response
- execution bridge examples linking `orderId` and `descriptorId`
